language: generic
# deprecated, no longer effect
#sudo: required
services:
  - docker

before_install:
  - docker build -t speudyland/docker-react -f Dockerfile.dev .

script:
  - docker run CI=true speudyland/docker-react npm run test -- --coverage